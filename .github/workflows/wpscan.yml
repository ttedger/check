- name: Run WPScan on dallolgebeya.com
  env:
    WPSCAN_API_TOKEN: ${{ secrets.WPSCAN_API_TOKEN }}
  run: wpscan --url https://dallolgebeya.com --enumerate vp,vt,u --api-token $WPSCAN_API_TOKEN --format json --output wpscan-results.json