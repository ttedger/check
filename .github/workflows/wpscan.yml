- name: Run WPScan on dallolgebeya.com
  env:
    WPSCAN_API_TOKEN: ${{ secrets.WPSCAN_API_TOKEN }}
  run: |
    set -x
    wpscan --url https://dallolgebeya.com --enumerate vp,vt,u --api-token $WPSCAN_API_TOKEN --format json --output wpscan-results.json
    echo "Exit code: $?"