name: WPScan Diagnostic

on:
  workflow_dispatch:

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Install WPScan
        run: sudo gem install wpscan

      - name: Basic scan (no token, no enumeration)
        run: |
          wpscan --url https://dallolgebeya.com --no-update